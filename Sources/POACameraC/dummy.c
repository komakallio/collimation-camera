/* POACameraC is a types-only Clang module. Function symbols are loaded
 * at runtime from libPlayerOneCamera.dylib and libPlayerOnePW.dylib. This
 * empty translation unit satisfies SwiftPM's requirement that a target
 * contain sources. */
